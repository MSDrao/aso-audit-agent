# Optional. When present, the ASO agent uses Mastra + OpenAI for richer copy recommendations.
# Without it, the app still runs end-to-end with a deterministic audit engine.
OPENAI_API_KEY=

# Optional model override for the Mastra audit agent.
ASO_AGENT_MODEL=openai/gpt-4o-mini

# Optional server port.
PORT=5173
